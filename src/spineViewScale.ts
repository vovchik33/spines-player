/** Spine display scale limits (slider + wheel zoom). */
export const SPINE_VIEW_SCALE_MIN = 0.1
export const SPINE_VIEW_SCALE_MAX = 3

/** Animation playback rate (Spine `AnimationState.timeScale` while playing). */
export const SPINE_ANIMATION_SPEED_MIN = 0.1
export const SPINE_ANIMATION_SPEED_MAX = 3
export const SPINE_ANIMATION_SPEED_STEP = 0.05
